# Institution 输出质检门禁

在最终回复前，必须逐项检查并全部通过：

1. 结构完整：包含查询结论，命中的机构画像/动态/科研成果段，必要时能力边界说明。
2. 优先级完整：所有展示条目都带 `P1/P2/P3`，且按 `P1 -> P2 -> P3` 排序。
3. 链接完整：
   - 动态/科研成果：必须给原始 `url`。
   - 机构画像：若无外部原始链接，必须标注“记录链接：/api/v1/institutions/{institution_id}”。
4. 近似过滤透明：凡是 `keyword/source_name` 或客户端二次过滤场景，必须明确“部分支持/近似方案”。
5. 不落地文件：默认直接回复，不创建本地 `.md`/`.markdown` 报告文件。
6. 长度受控：
   - Standard 模式（默认）：每个段落最多 `P1<=6`、`P2<=4`、`P3<=2`。
   - Fast 模式（按需）：每个段落最多 `P1<=3`、`P2<=2`、`P3<=1`。

若任一项不满足，先修正再发送。
